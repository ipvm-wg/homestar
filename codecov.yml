ignore:
  - "ipvm/tests"
  - "ipvm/src/test_utils"
  - "ipvm-guest-wasm"
  - "ipvm-wasm/src/test_utils"

comment:
  layout: "reach, diff, flags, files"
  require_changes: true

github_checks:
  annotations: false

coverage:
  status:
    project:
      default:
        threshold: 5%
